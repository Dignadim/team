<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.board.free.FreeboardCommentVO">

	<insert id="insertComment" parameterType="fbc_vo">
		insert into fb_boardcomment (fbc_idx, fbc_gup, fbc_content) 
			values (fb_boardcomment_idx_seq.nextval, #{fbc_gup}, #{fbc_content})
	</insert>
	
	<select id="fb_commentCount" parameterType="int" resultType="int">
		select count(*) from fb_boardcomment where fbc_gup = #{fbc_idx}
	</select>
	
	<select id="selectCommentList" parameterType="int" resultType="fbc_vo">
		select * from fb_boardcomment where fbc_gup = #{fbc_idx} order by fbc_idx desc
	</select>
	
	<update id="updateComment" parameterType="fbc_vo">
		update fb_boardcomment set fbc_content = #{fbc_content} where fbc_idx = #{fbc_idx}
	</update>
	
	<delete id="deleteComment" parameterType="fbc_vo">
		delete from fb_boardcomment where fbc_idx = #{fbc_idx}
	</delete>

</mapper>














